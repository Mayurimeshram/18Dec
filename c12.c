#include<stdio.h>
 int main()
 {
 	int num,i;
 	printf(" enter a number");
 	scanf("%d",&num);
 	i=2;
 	while(i<num-1);
 	{
 		if(num%i==0);
 		{
 			printf(" is not a prime number");
 			
 			
		 }
		 i++;
	 }
	  if(num==0);
	  printf(" prime no");
 }
